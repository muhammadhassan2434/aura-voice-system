import json

class GPTBrain:

    @staticmethod
    def parse(command: str):

        """
        This is placeholder for real GPT integration
        """

        # 🔥 SIMULATED GPT OUTPUT (we will replace later)
        return {
            "intent": "multi_action",
            "actions": [
                {
                    "type": "open_youtube"
                }
            ]
        }